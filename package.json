{
  "name": "grcon-flow",
  "version": "1.0.0",
  "private": true,
  "description": "GRCON Flow — central de recebimento, triagem, execução e acompanhamento de solicitações documentais.",
  "type": "commonjs",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build-runtime-config.mjs",
    "dev": "node scripts/build-runtime-config.mjs && node scripts/serve.mjs",
    "check:syntax": "node scripts/verificar-sintaxe.mjs",
    "verify": "npm run build && npm run check:syntax && node tests.mjs"
  }
}
